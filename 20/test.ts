
const a = []
console.log("[] is", a?.length ? "yes" : "no")
console.log("[] is", a?.length > 0 ? "yes" : "no")

const b = [1]
console.log("[] is", b?.length ? "yes" : "no")
console.log("[] is", b?.length > 0 ? "yes" : "no")

console.log("[] is", c?.length ? "yes" : "no")
console.log("[] is", c?.length > 0 ? "yes" : "no")